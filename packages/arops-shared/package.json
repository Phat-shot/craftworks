{
  "name": "@craftworks/arops-shared",
  "version": "0.1.0",
  "description": "AR Ops shared core: geo math, hit validation, geofence logic. Pure TypeScript, zero runtime dependencies. Used identically by server (Node) and app (React Native).",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "tsc && node --test \"dist/test/*.test.js\""
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.6.0"
  }
}
